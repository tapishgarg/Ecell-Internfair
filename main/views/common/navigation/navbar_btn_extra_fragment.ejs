<div class="btn-extars">
    <!-- <a href="#" title="" class="post-job-btn"><i class="la la-plus"></i>Post Jobs</a> -->
    <ul class="account-btns">
        <!-- <div class="g-signin2" data-onsuccess="onSignIn"></div> -->
        <li><a title="" href="/startup/manage" v-if="is_startup_registered && is_startup_verified && !is_loading"><i
                        class="fas fa-building"></i>Dashboard</a>
        <li class="signup-popup"><a title="" v-if="!is_student_registered && !is_loading && !is_startup_verified"><i
                        class="fas fa-building"></i> Startups</a></li>
        <li class="signin-popup"><a title="" v-if="!is_student_registered && !is_loading && !is_startup_registered"><i
                        class="fas fa-user-graduate"></i> Student</a></li>
        <li><a title="" href="/student/profile" v-if="is_student_registered && !is_loading && !is_startup_registered"><i
                        class="fas fa-user-graduate"></i> Dashboard</a></li>
        <li><a title="" onclick="signOut()" v-if="is_signed_in"><i class="fas fa-sign-out-alt"></i> Sign Out</a>
        </li>
    </ul>
</div>
